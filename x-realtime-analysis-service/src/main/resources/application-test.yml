server:
  port: 8040

spring:
  data:
    redis:
      port: 6379
      host: 10.33.93.12

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.33.93.12:13306/x?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root

  kafka:
    bootstrap-servers: 10.33.93.12:9092

logging:
  level:
    com.x.realtime.analysis.service: info
    # Flink 核心包日志级别改为 DEBUG，输出算子处理细节
    #org.apache.flink.streaming.runtime.tasks: DEBUG
    #org.apache.flink.streaming.api.operators: DEBUG
    # 你的包日志级别改为 DEBUG，查看数据处理过程
    #com.x.realtime.analysis.service.flink: DEBUG
    org.apache.flink.*: warn

rabbitmq:
  host: 10.33.93.12
  port: 5672
  username: guest
  password: guest
  virtualHost: /
  alertQueue: sensor.alert.queue
  connectionTimeout: 5000
  retryCount: 3 # 重试 3 次
  retryInterval: 1000 # 每次重试间隔 1 秒（指数退避：1s→2s→3s）