server:
  port: 8088

spring:
  application:
    name: realtime-analysis-service
    
kafka:
  bootstrap-servers: localhost:9092
  
flink:
  job-manager: localhost:8081
  
redis:
  host: localhost
  port: 6379

logging:
  level:
    com.x.realtime.analysis.service: debug